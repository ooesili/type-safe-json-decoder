box: node

dev:
  steps:
  - npm-install
  - script:
      code: npm run typings
  - internal/watch:
      code: npm test
      reload: true

build:
  steps:
  - npm-install
  - script:
      code: npm run typings
  - npm-test

box: node

dev:
  steps:
  - npm-install
  - script: &typings
      name: typings install
      code: npm run typings
  - internal/watch:
      code: npm test
      reload: true

build:
  steps:
  - npm-install
  - script: *typings
  - npm-test
